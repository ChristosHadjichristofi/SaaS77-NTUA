<nav class="navbar navbar-light navbar-expand-md sticky-top bg-white" id="header-el">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">&nbsp;
            <img src="/assets/img/logo1.1.png" id="imagelogo" width="200" height="50" />
        </a>
        
        <button data-toggle="collapse" class="navbar-toggler" data-target="#navcol-1">
            <span class="sr-only">Toggle navigation</span>
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navcol-1">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item"></li>
                <% if(pageTitle === "Profile Page" && authenticated) { %>
                <li class="nav-item">
                    <div style="display:flex">
                        <a class="btn btn-secondary" role="button" id="redirect-button" href="home">
                            <i class="fa fa-home" id="icons-nav"> Home</i>
                        </a>
                        <form method="POST" action="/account/signout">
                            <button class="btn btn-primary" type="submit">Sign Out</button>
                        </form>
                    </div>
                </li>
                <% } else if (pageTitle === "Home Page" && authenticated) { %>
                <li class="nav-item">
                    <div style="display:flex">
                        <a class="btn btn-secondary" role="button" id="redirect-button" href="profile">
                            <i class="fa fa-user" id="icons-nav"> Profile</i>
                        </a>
                        <form method="POST" action="/account/signout">
                            <button class="btn btn-primary" type="submit">Sign Out</button>
                        </form>
                    </div>
                </li>
                <% } else if (pageTitle === "Browse Questions Page" && authenticated) { %>
                <li class="nav-item">
                    <a class="btn btn-secondary" role="button" id="redirect-button" href="/home">
                        <i class="fa fa-home" id="icons-nav"> Home</i>
                    </a>
                    <a class="btn btn-secondary" role="button" id="redirect-button" href="/profile">
                        <i class="fa fa-user" id="icons-nav"> Profile</i>
                    </a>
                </li>
                <% } else { %>
                <li class="nav-item">
                    <a class="nav-link active" href="#">
                        <button class="btn btn-outline-primary" data-toggle="modal" data-target="#signin" type="button">Log In</button>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" href="#">
                        <button class="btn btn-primary" data-toggle="modal" data-target="#signup" type="button">Sign Up</button>
                    </a>
                </li>                
                <% } %>
            </ul>
        </div>
    </div>
</nav>
